import styled from "styled-components";

const Footer = () => {

    return (
        <StyledFooter>
            푸터
        </StyledFooter>
    );
};

const StyledFooter = styled.footer`
    width: 100%;
`;

export default Footer;