import styled from "styled-components";

const Menu = () => {

    return (
        <Div>
            Menu
        </Div>
    );
};

const Div = styled.div`
    background: green;
`;

export default Menu;